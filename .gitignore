__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.coverage
htmlcov/

# Foreman per-target state and init output (gitignore has no inline comments,
# so these must each be on their own line).
.foreman/
.claude/
